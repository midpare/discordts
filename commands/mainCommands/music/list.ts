import { commandType } from "../../../typings/command";

export = <commandType> {
  name: 'list',
  category: 'music',
  use: 'list',
  description: '현재 노래 리스트를 확인합니다.',
  execute: async ({msg, args}) => {

  }
}